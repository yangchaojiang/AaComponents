<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aac">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="App"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/MainTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".data.activity.TestDataActivity"/>
        <activity android:name=".data.activity.TestDataListActivity"
            android:theme="@style/AppThemes"

            />
        <activity android:name=".data.fragment.list.defaults.TestDataFragmentActivity"
        android:theme="@style/AppThemes"
        />
        <activity android:name=".data.fragment.data.Test2FragmentActivity"
            android:theme="@style/AppThemes"

            />
        <activity android:name=".data.fragment.TestFragmentActivity"
            />
        <activity android:name=".data.fragment.list.custom.TestCustomFragmentActivity"
            android:theme="@style/AppThemes"
            />
    </application>

</manifest>